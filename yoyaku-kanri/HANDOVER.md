# Beaufield 予約管理アプリ — 引き継ぎドキュメント

## 概要

| 項目 | 内容 |
|------|------|
| アプリ名 | Beaufield 予約管理アプリ |
| 作業ディレクトリ | `開発・自動化/yoyaku-kanri/` |
| 技術構成 | GAS + Google Sheets + index.html（SPA） |
| ホスティング | GitHub Pages（beaufield/beaufield-dev） |
| 認証 | beaufield-auth（既存の bf_session 共通セッション） |

---

## バージョン状況（2026-04-09時点）

| ファイル | バージョン | 状態 |
|----------|-----------|------|
| gas/Code.gs | **v1.6.0** | ✅ GASデプロイ済み |
| index.html | **v1.10.0** | ✅ GitHub Pages反映済み |

---

## ファイル構成

```
yoyaku-kanri/
├── gas/
│   └── Code.gs       ← GASにコピーするバックエンド
├── index.html        ← GitHub Pages に push するフロントエンド
└── HANDOVER.md       ← このファイル
```

---

## GAS接続情報

| プロパティ名 | 値 |
|-------------|-----|
| `SPREADSHEET_ID` | ※GASスクリプトプロパティに設定済み（直書き禁止） |
| `AUTH_SHEET_ID` | `1cCQn16ubEN_Af7XWw8KerBscZtFomBnXHjIIiZUr6V8`（beaufield-auth 共通） |

**GAS WebApp URL:**
```
https://script.google.com/macros/s/AKfycbxvWbUf17DVe6dpW8cHEqrCuA3BbqF49EXqU3JXkA7hIR5lcT5sZwlQtgJPSi8rKTeo3w/exec
```

---

## 権限設計（user_app_rolesシート）

アクセス権限は `beaufield-auth` の `user_app_roles` シートで管理。
`is_admin` カラムとは独立しており、このアプリのみの権限設定。

| role | 対象 | できること |
|------|------|------------|
| `admin` | 事務スタッフ | 全予約閲覧・編集・削除、ステータス変更（確定/予約に戻す）、商品管理、集計、入荷処理 |
| `staff` | 営業スタッフ | 自分の予約のみ登録・変更・削除（「予約」ステータスのみ）、全件閲覧、集計閲覧 |
| 未登録 | — | アクセス不可（ACCESS_DENIED） |

**ユーザー登録方法:** `user_app_roles` シートに `user_id / yoyaku-kanri / admin or staff` を追記。

---

## 画面構成

### 全ユーザー共通タブ
| タブ | 内容 |
|------|------|
| 📝 予約登録 | サロン名・商品・数量・発送方法・担当者を入力して登録 |
| 📋 予約一覧 | フィルタ（商品・担当・ステータス）、印刷(PDF)、カードタップで編集 |
| 📊 集計 | 商品別・担当別・サロン別集計、クロス集計マトリクス |

### admin（事務）のみ
| 機能 | 備考 |
|------|------|
| 🏷️ 商品管理タブ | 商品追加・編集・有効/無効切り替え |
| 確定/予約に戻すボタン | 予約一覧の各カードに表示 |
| 📦 入荷処理ボタン | 予約一覧のフィルタ行に表示 |
| 👤 ユーザー管理 | ポータル経由 |

---

## 入荷処理機能（v1.8.0〜）

### フロー
1. 予約一覧タブ → 📦入荷処理ボタン（admin限定）
2. Step1: 商品選択（予約ありのもの） + 入荷数を入力
3. Step2: プレビューテーブル（No昇順で自動割り当て）
   - 各行のON/OFFチェック・確定数の手動調整可能
   - リアルタイムカウンター（入荷数 / 割り当て済 / 残り）
   - 超過時はエラー表示・実行ボタン無効化
4. 実行 → 確定フィルタで一覧を表示

### スプリット処理（分割確定）
- 予約数の一部だけ確定する場合：
  - **元行**: 数量を減らす（ステータスは「予約」のまま、Noは変えない → 優先順位保持）
  - **新行**: 確定分を新Noで追加（サロン名・担当・発送方法・予約日時をコピー）

---

## 予約編集ルール

| ルール | 詳細 |
|--------|------|
| 編集方法 | 予約カードをタップして編集モーダルを開く（編集ボタンは廃止） |
| **数量増加禁止** | 現在の予約数より増やすことは不可（フロント+GAS両側でバリデーション） |
| 数量減少 | 可能 |
| 理由 | 数量増加は予約優先順位の公平性を損なうため |

---

## 速度改善対策（v1.6.0 / v1.10.0）

### GAS側
- **keepWarm()関数**: GASコールドスタート（15〜30秒）を防止
  - **GASエディタ → トリガー → keepWarm → 5分おき** で設定済み
  - `PropertiesService` を1回読むだけの軽量関数。課金・クォータ消費はほぼゼロ
- `CACHE_TTL_AUTH`: 5分 → 15分（認証キャッシュ延長）
- `_getUsersFromAuth`: 10分キャッシュ追加（AUTH_SHEET_ID の二重読み込みを解消）

### フロント側
- **localStorageキャッシュ** (`bf_yoyaku_v1100`, TTL=5分)
  - キャッシュあり → 即表示、バックグラウンドで予約を最新化
  - キャッシュなし → GASから全取得後キャッシュ保存（次回から即表示）
  - 予約の変更操作後はキャッシュも自動更新

---

## データ構造

### products シート（商品マスター）

| 列 | 内容 | 備考 |
|----|------|------|
| A | product_id | 自動生成（P + timestamp） |
| B | name | 商品名 |
| C | stock_limit | 予約上限数（0=無制限） |
| D | deadline | 受付期限（YYYY-MM-DD） |
| E | is_active | TRUE/FALSE |
| F | created_at | 登録日時 |

### reservations シート（予約データ）

| 列 | 内容 | 備考 |
|----|------|------|
| A | reservation_no | 予約No（最大No+1で発番、欠番あり） |
| B | salon_name | サロン名 |
| C | product_id | 商品ID |
| D | product_name | 商品名（登録時コピー） |
| E | quantity | 数量 |
| F | status | 予約 / 確定 |
| G | staff_id | 担当者 user_id |
| H | staff_name | 担当者名 |
| I | operator_id | 操作者 user_id |
| J | operator_name | 操作者名 |
| K | delivery_method | 発送 / 持参 / 未定 |
| L | reserved_at | 予約日時 |
| M | updated_at | 更新日時 |

---

## 再デプロイ手順（コード変更後）

1. GAS エディタで「デプロイ」→「デプロイを管理」
2. 鉛筆アイコン（編集）をクリック
3. バージョンを「**新バージョン**」に変更
4. 「デプロイ」— URL は変わらないのでそのまま使える

---

## このセッションで実装した主な機能

| バージョン | 内容 |
|-----------|------|
| index v1.8.0 / Code v1.5.0 | **入荷処理機能**（2ステップモーダル・スプリット処理） |
| index v1.9.0 / Code v1.5.1 | カードタップ編集・数量増加禁止・確定ボタンadmin限定明示 |
| index v1.10.0 / Code v1.6.0 | **速度改善**（keepWarm・CacheService・localStorageキャッシュ） |

---

## 次回セッション開始コマンド

```
Beaufield 予約管理アプリの続きです。
yoyaku-kanri/HANDOVER.md を参照して進めてください。
```

---

## 今後の検討事項

| 項目 | 内容 |
|------|------|
| LINE WORKS通知 | 入荷処理完了時や上限到達時にWebhookで担当者に通知 |
| 印刷レイアウト改善 | 商品フィルタ選択中は商品名列を省略するなど |
| 一括入荷処理 | 複数商品をまとめて処理できるよう拡張（現在は1商品ずつ） |
